flask hello world:

you can change manage.py's configure
start app: python manage.py dev

this example dir like rails, hope you known ruby and rails(but there is not migration shell)

the dir structure important, code nothing